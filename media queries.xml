/* Soon afer variable definitions*/

/* Content
----------------------------------------------- */

body { display:block; }
@media screen and (max-width: 699px) and (min-width: 320px) and (orientation:portrait){
  body {  
    height: 100%;
	width: 100%;
    -webkit-transform: rotate(90deg);
    -moz-transform: rotate(90deg);
    -o-transform: rotate(90deg);
    -ms-transform: rotate(90deg);
    transform: rotate(90deg);
  }
  .content{
    margin-top: 30%;
    zoom: 1;
	max-height: 800px;
	width: 100%;
  }
  .content-outer, .content-fauxcolumn-outer, .region-inner{
  	width: 100%;
  }
}

/* just before styling */
body {
  font: $(body.font);
  color: $(body.text.color);
  background: $(body.background);
  padding: 0 0 0 0;
  $(body.background.override)
}
